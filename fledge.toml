[tasks]
spec = "specsync check --strict --force --require-coverage 100"
clippy = "cargo clippy -- -D warnings"
rust-test = "cargo test"
bun-test = "bun test"
[lanes.verify]
description = "Run Corvid Hex Rust lint and Rust/Bun regression suites"
steps = ["spec", "clippy", "rust-test", "bun-test"]
