---
change: CHG-0003-complete-the-specsync-5-and-trust-1-migration-with-strict-trust-enforcement-com
artifact: requirements
---

# Requirements

1. Trust SHALL use the strict profile so SpecSync warnings remain blocking in the unified hosted gate.
2. SDD meaningful paths SHALL include `.trust.toml`, `fledge.toml`, `.augur.toml`, `.attest.json`, and `AGENTS.md`.
3. Every pre-existing acceptance criterion in the ten canonical requirement companions SHALL receive one deterministic module-scoped stable ID without altering product behavior.
4. Released SpecSync 5.0.1 SHALL pass both normal and forced strict checks at 100 percent.
5. Repository-native Clippy, Rust, and genuine Bun verification SHALL pass before acceptance.
